<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- 深色主题资源 -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="#2D2D30"/>
    <SolidColorBrush x:Key="ForegroundBrush" Color="#F1F1F1"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#3399FF"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#444444"/>
    <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#252526"/>
    <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="HeaderForegroundBrush" Color="#F1F1F1"/>
    <SolidColorBrush x:Key="ContentBackgroundBrush" Color="#1E1E1E"/>
    
    <!-- 控件样式 -->
    <Style TargetType="Page">
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
    </Style>
    
    <Style TargetType="Border">
        <Setter Property="Background" Value="#333337"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>
    
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
    </Style>
    
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
    </Style>
    
    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Background" Value="#3E3E42"/>
    </Style>
    
</ResourceDictionary> 